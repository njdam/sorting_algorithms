#include "deck.h"

/**
 * sort_deck - is func to sort a deck of cards;
 * @deck: is a head node of a deck_node_t doubly-linked list;
 *
 * Return: nothing.
 */
void sort_deck(deck_node_t **deck)
{
	if (!deck || !(*deck) || !((*deck)->next))
		return;
	else
		return;
}
